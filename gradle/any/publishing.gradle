apply plugin: 'maven-publish'

publishing {
    repositories {
        maven {
            url 'https://pkgs.dev.azure.com/RPSGroupApps/IOOS-TDS/_packaging/tds/maven/v1'
            name 'tds'
            authentication {
                basic(BasicAuthentication)
            }
        }
    }
}
